[package]
name = "core_payment_plan"
version = "0.1.0"
edition = "2021"

[lib]
name = "core_payment_plan"
path = "src/lib.rs"

[dependencies]
xirr = { version = "0.3.2", features = ["chrono"] }
chrono = { version = "0.4.38", features = ["serde"] }
serde = { version = "1.0.203", features = ["serde_derive"] }

[features]
simple = []
iterative = []